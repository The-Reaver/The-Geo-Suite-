"""Route package for the backend.

This package holds every FastAPI router the app mounts. Each module
exposes one APIRouter that backend/app/main.py includes at startup.
Add new route modules here as the platform grows, then include their
routers in main.py so the app serves them.
"""

